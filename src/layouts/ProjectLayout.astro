---
import BaseLayout from './BaseLayout.astro';
import type { CollectionEntry } from 'astro:content';

interface Props {
  project: CollectionEntry<'work'>;
}

const { project } = Astro.props;
const { title, description, date, tags, image, link } = project.data;

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
});
---

<BaseLayout title={title} description={description}>
  <article class="mx-auto max-w-3xl px-6 py-16 gxsc-reveal">
    <header class="mb-12 gxsc-reveal gxsc-delay-1">
      <div class="flex items-center gap-3 text-sm text-[var(--color-text-muted)] mb-4">
        <time datetime={date.toISOString()}>{formattedDate}</time>
        {tags.length > 0 && (
          <>
            <span>·</span>
            <div class="flex flex-wrap gap-2">
              {tags.map((tag) => (
                <span class="px-2 py-1 bg-[var(--color-bg-primary)] text-[var(--color-text-muted)] rounded-full text-xs border border-[var(--color-border)]">
                  {tag}
                </span>
              ))}
            </div>
          </>
        )}
      </div>
      <h1 class="text-4xl font-semibold text-[var(--color-text-primary)] leading-tight font-display">
        {title}
      </h1>
      {description && (
        <p class="mt-4 text-xl text-[var(--color-text-secondary)]">
          {description}
        </p>
      )}
      {link && (
        <a
          href={link}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 mt-6 text-[var(--color-accent)] hover:underline"
        >
          View project
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
          </svg>
        </a>
      )}
    </header>
    {image && (
      <img
        src={image}
        alt={title}
        class="w-full rounded-[10px] mb-12 border border-[var(--color-border)]"
      />
    )}
    <div class="prose max-w-none prose-headings:font-semibold prose-a:no-underline hover:prose-a:underline">
      <slot />
    </div>
    <footer class="mt-16 pt-8 border-t border-[var(--color-border)]">
      <a href="/work" class="text-[var(--color-accent)] hover:underline">
        &larr; Back to all work
      </a>
    </footer>
  </article>
</BaseLayout>
